# Git LFS Configuration for ArtSiteMaker
# Note: Enable these lines when Git LFS is installed on the system

# Large image files to be stored in LFS (disabled until LFS is installed)
# *.webp filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text
# *.jpeg filter=lfs diff=lfs merge=lfs -text
# *.png filter=lfs diff=lfs merge=lfs -text
# *.tiff filter=lfs diff=lfs merge=lfs -text
# *.tif filter=lfs diff=lfs merge=lfs -text
# *.heic filter=lfs diff=lfs merge=lfs -text
# *.heif filter=lfs diff=lfs merge=lfs -text

# Keep thumbnails in regular Git (small PNGs for quick access)
# thumbnails/*.png !filter !diff !merge text=auto
